import { render } from '@testing-library/react';

import ListPage from './ListPage';

describe('<ListPage /> test suite', () => {
  it('Should render the <ListPage /> component', () => {
    render(<ListPage />);
  });
});
